import type { NextConfig } from "next";

// Standalone Vercel project (second-brain-final). No basePath —
// multi-zone under charandeepkapoor.com is a separate concern.
const nextConfig: NextConfig = {};

export default nextConfig;
