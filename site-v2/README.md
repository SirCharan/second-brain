# second-brain — cinema landing (site-v2)

Ground-up redesign of the public landing. **Does not touch** the original `site/` app.

## Direction

**Halide cinema** — tungsten darkroom for local AI memory.

- Signature artifact: interactive **memory orrery** (canvas constellation)
- Type: Gambarino (display) + IBM Plex Sans / Mono
- Palette: near-black + amber accent + cool slate (no purple)

## Dev

```bash
cd site-v2
npm install
npm run dev
```

## Deploy

This folder is a separate Vercel project root (`site-v2`). Do not redeploy the existing `site` project from here.
