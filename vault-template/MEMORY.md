# MEMORY — index of memory notes

Thin table of contents, regenerated by `regen-index.py`. Each folder's full note list
lives in its `_index-<folder>` shard. `MEMORY.md`, `context.md`, `_session-log.md` stay at root.

## 🗂️ Notes
- [[_index-example]] — example folder (1)
