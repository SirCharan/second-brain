# the vault is the USER's data — never commit a real one
second-brain-vault/

# a mined eval set is the user's own prose, lifted out of their capture journal.
# Commit the miner (tests/eval/build_set.py), never its output.
tests/eval/*.jsonl
tests/eval/*baseline*.json
!tests/eval/queries.example.jsonl
*.bak
__pycache__/
*.pyc
.DS_Store
hook-errors.log
.embed/
.recall-state/
.ruff_cache/

# build artifacts + local video/demo output (not shipped machinery)
node_modules/
.next/
**/node_modules/
**/.next/
brag-output/
brag-output-*/
launch/
site-final/launch-video/
